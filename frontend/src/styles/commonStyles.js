export const commonStyles = {
  // 主标题样式 (H1)
  mainTitle: "text-2xl font-bold text-gray-900",
  
  // 二级标题样式 (H2)
  secondaryTitle: "text-xl font-bold text-gray-900",
  
  // 三级标题样式 (H3)
  tertiaryTitle: "text-lg font-semibold text-gray-900",
  
  // 按钮文字样式
  buttonText: "text-base font-medium",
  
  // 正文文字样式
  bodyText: "text-base text-gray-900",
  
  // 辅助文字样式
  auxiliaryText: "text-sm text-gray-600",
  
  // 页面容器样式
  pageContainer: "min-h-screen bg-gray-50 font-sans",
  
  // 内容区域样式
  contentArea: "w-full mx-auto p-6",
  
  // 卡片样式
  card: "bg-white border border-gray-300 rounded-lg shadow-sm",
  
  // 输入框样式
  input: "w-full px-4 py-3 border border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-blue-500 focus:border-blue-500 text-gray-900 bg-white",
  
  // 文本域样式
  textarea: "w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-blue-500 focus:border-blue-500 resize-vertical text-gray-900 bg-white",
  
  // 主要按钮样式 - 增加最小宽度确保文字水平显示
  primaryButton: "min-w-24 px-8 py-3 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition-colors font-medium whitespace-nowrap",
  
  // 次要按钮样式
  secondaryButton: "px-4 py-2 bg-gray-100 text-gray-700 rounded-md hover:bg-gray-200 transition-colors font-medium",
  
  // 导航按钮样式
  navButton: "px-4 py-2 text-base font-medium transition-colors"
};